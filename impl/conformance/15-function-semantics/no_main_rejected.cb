// CONFORMANCE-CASE
// spec: 15-function-semantics
// facility: rule.fn.program [Program-No-Main]
// kind: negative
// expect: diag.no-main (static)
//
// conf.no-main (CHG-0028): a program must declare `fn main()` at the
// root; a `main` inside a module is the ordinary item `m::main`.

module m
{
    export fn main() : i32
    {
        0
    }
}

fn helper()
{
}
