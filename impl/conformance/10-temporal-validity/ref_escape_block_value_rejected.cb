// CONFORMANCE-CASE
// spec: 10-temporal-validity
// facility: rule.temporal.ref-escape [Ref-Escape-Rejected] (block result stored by a declaration)
// kind: negative
// expect: diag.reference-escapes-scope (static)
//
// The borrow is the inner block's result and is stored by the outer
// declaration, whose decl-block strictly encloses the referent's.

fn main()
{
    auto r =
    {
        i32 y = 2;
        &y
    };
}
