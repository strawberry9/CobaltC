// CONFORMANCE-CASE
// spec: 10-temporal-validity
// facility: rule.temporal.elision [Call-Multi-Ref-Return-Rejected]; conf.elision-multi-param-rejected
// kind: negative
// expect: diag.lifetime-elision-ambiguous (static)
//
// A function with two reference-typed parameters returning a reference
// is rejected at its declaration: the language will not guess which
// argument the result depends on, and there are no lifetime
// annotations to say. A declaration-level fact, so the program is
// rejected whether or not the result would actually dangle.

fn pick(ref<i32, shared> p1, ref<i32, shared> p2) : ref<i32, shared>
{
    p2
}

fn main()
{
    i32 a = 1;
    ref<i32, shared> result = &a;
    {
        i32 b = 2;
        result = pick(&a, &b);
    }
    if (*result != 2)
    {
        1 / (1 - 1);
    }
}
