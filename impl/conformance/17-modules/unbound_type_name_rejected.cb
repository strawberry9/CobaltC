// CONFORMANCE-CASE
// spec: 17-modules
// facility: rule.module.resolve [Resolve-Unbound]
// kind: negative
// expect: diag.unbound-name (static)
//
// A type name that names no item is unbound, in a signature as anywhere
// else. `Mutex` is not a type (the built-in is `mutex`); the checker once
// let an unknown name in a parameter type through, and cobc then failed
// on it with an internal error.

import std;

fn worker(ref<Mutex<i64>, shared> m)
{
}

fn main()
{
}
