// CONFORMANCE-CASE
// spec: 17-modules
// facility: rule.module.file [Module-File]; rule.fn.program
// kind: positive
// expect: ok
//
// conf.module-file-main-is-ordinary-ok (CHG-0026): a `main` in the
// loaded file is the item `lib::main`, not the program's entry; this
// file's root `main` is.

module lib "./fixtures/has_main.cb";

fn main()
{
    if (lib::main() != 7)
    {
        1 / (1 - 1);
    }
}
