// CONFORMANCE-CASE
// spec: 17-modules
// facility: rule.module.file [Module-File]; rule.module.use
// kind: positive
// expect: ok
//
// conf.module-file-import-ok (CHG-0026): `import` is untouched by the
// file mechanism -- it shortens lookup into the loaded module exactly
// as it does into an inline one.

module geo "./fixtures/geometry.cb";

import geo::square;

fn main()
{
    if (square(3.0) != 9.0)
    {
        1 / (1 - 1);
    }
}
