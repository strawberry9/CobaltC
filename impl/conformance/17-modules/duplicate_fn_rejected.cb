// CONFORMANCE-CASE
// spec: 17-modules
// facility: rule.module.resolve [Item-Duplicate]
// kind: negative
// expect: diag.duplicate-item (static)
//
// conf.duplicate-fn-rejected (CHG-0032): two declarations add the same
// qualified name `f`; `Σ.items` maps a name to one item, so the program
// is ill-formed. Reported at the second declaration.

import std;

fn f()
{
    printf("first\n");
}

fn f()
{
    printf("second\n");
}

fn main()
{
    f();
}
