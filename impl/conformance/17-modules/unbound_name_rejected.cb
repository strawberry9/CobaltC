// CONFORMANCE-CASE
// spec: 17-modules
// facility: rule.module.resolve [Resolve-Unbound]
// kind: negative
// expect: diag.unbound-name (static)
//
// A name that resolves to nothing in any visible scope is rejected
// before the program runs.

fn main()
{
    does_not_exist();
}
