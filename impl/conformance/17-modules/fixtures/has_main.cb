// CONFORMANCE-FIXTURE
// A file body that declares a `main`. As the body of a module it is
// the ordinary item `<module>::main`, not the program's entry
// (spec/17 §5; rule.fn.program wants the program's `main` at the root).

export fn main() : i32
{
    7
}
