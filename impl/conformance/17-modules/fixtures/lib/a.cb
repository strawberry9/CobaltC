// CONFORMANCE-FIXTURE
// Declares its own file-backed module with a path relative to *this*
// file's directory (spec/17 §5: "a file-backed declaration inside p
// resolves its own path against p's directory"). `export` on the
// declaration makes `b` reachable from wherever `a` is.

export module b "./b.cb";
