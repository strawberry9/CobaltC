// CONFORMANCE-CASE
// spec: 17-modules
// facility: rule.module.resolve [Assoc-Fn-Foreign-Type]
// kind: negative
// expect: diag.foreign-associated-fn (static)
//
// conf.foreign-assoc-fn-rejected (CHG-0033): `fn T::name` adds `M::T::name`
// only where `T` is declared in `M` (`spec/17` §1). `Vec` is declared in
// `std`, so the root cannot add `Vec::first_or`, nor redeclare `Vec::drop`.

import std;

fn Vec::first_or<T>(ref<Vec<T>, shared> v, T d) : T
{
    d
}

fn main()
{
}
