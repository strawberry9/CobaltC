// CONFORMANCE-CASE
// spec: 17-modules
// facility: rule.module.resolve [Resolve-Unbound]
// kind: negative
// expect: diag.unbound-name (static)
//
// conf.unbound-qualified-name-static: a qualified path can never be a
// local binding, so a path that names no item of its prefix is
// rejected before the program runs.

module m
{
    export fn present() : i32
    {
        1
    }
}

fn main()
{
    auto x = m::absent();
}
