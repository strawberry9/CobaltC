// CONFORMANCE-CASE
// spec: 17-modules
// facility: rule.module.file [Module-File]
// kind: positive
// expect: ok
//
// conf.module-file-ok (CHG-0026): a module whose body is another file,
// reached by qualified path. `geo` is named here; geometry.cb's top
// level is anonymous, exactly as an inline body would be.

module geo "./fixtures/geometry.cb";

fn main()
{
    if (geo::square(3.0) != 9.0)
    {
        1 / (1 - 1);
    }
}
