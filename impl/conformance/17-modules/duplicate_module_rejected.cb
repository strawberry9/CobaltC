// CONFORMANCE-CASE
// spec: 17-modules
// facility: rule.module.resolve [Item-Duplicate] (module)
// kind: negative
// expect: diag.duplicate-item (static)
//
// conf.duplicate-module-rejected (CHG-0032): two `module m` declarations
// are two items named `m`, not one module in two parts.

module m
{
    export fn f()
    {
    }
}

module m
{
    export fn g()
    {
    }
}

fn main()
{
}
