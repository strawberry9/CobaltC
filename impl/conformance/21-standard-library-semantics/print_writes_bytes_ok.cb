// CONFORMANCE-CASE
// spec: 21-standard-library-semantics
// facility: rule.stdlib.str [Str-Ptr]; [Str-Len]; print; rule.trust.extern-call [Extern-Call]; conf.print-observed
// kind: positive
// expect: ok
// expect-stdout-hex: 68690a
//
// `print` writes exactly a str's bytes through the `write` extern, and
// the program itself contains no `unsafe`: the trust transition is
// inside the prelude function.

import std;

fn main()
{
    printf("hi\n");
}
