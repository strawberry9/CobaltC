// CONFORMANCE-CASE
// spec: 20-trust-boundaries
// facility: rule.trust.extern-call [Extern-Non-Ffi-Type]
// kind: negative
// expect: diag.extern-non-ffi-type (static)
//
// FfiType ::= integer types | f32 | f64 | bool | void | rawptr<tau>.
// An extern fn declared with a resource-typed parameter (Vec<i32>,
// not FfiType) was previously silently accepted. Checked for every
// declared extern unconditionally, whether or not it is ever called.

import std;

extern fn g(Vec<i32> v);

fn main()
{
}
