// CONFORMANCE-CASE
// spec: 14-control-flow
// facility: rule.control.while
// kind: negative
// expect: diag.break-outside-loop (static)
//
// `break` lexically outside any enclosing `while` was previously
// unrejected at the registered diagnostic: it fell through to a
// generic internal "unexpected control flow at top level" message
// instead of `diag.break-outside-loop`.

fn main()
{
    break;
}
