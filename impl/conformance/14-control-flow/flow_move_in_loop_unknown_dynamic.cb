// CONFORMANCE-CASE
// spec: 14-control-flow
// facility: rule.control.flow-analysis (loop fixed point: valid(v) joins to ?)
// kind: negative
// expect: diag.stale-binding (dynamic)
//
// On the first pass through the body `valid(v)` is T; the back edge
// brings F; the head is `?` at the fixed point, so the second
// iteration's use is not refuted statically and faults at run time.

import std;

fn takes(Vec<i32> v)
{
}

fn main()
{
    Vec<i32> v = Vec::new();
    i32 i = 0;
    while (i < 3)
    {
        takes(v);
        i = i + 1;
    }
}
