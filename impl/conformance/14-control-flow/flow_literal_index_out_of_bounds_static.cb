// CONFORMANCE-CASE
// spec: 14-control-flow
// facility: rule.control.flow-analysis (value-range row: literal index); rule.agg.index [Index-Checked]; conf.index-out-of-bounds-static
// kind: negative
// expect: diag.index-out-of-bounds (static)
//
// A literal index against a fixed array length is a value-range
// condition the analysis decides from the literals alone.

fn main()
{
    array<i32, 3> a = [1, 2, 3];
    i32 y = a[3];
}
