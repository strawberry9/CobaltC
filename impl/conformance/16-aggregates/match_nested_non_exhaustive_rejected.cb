// CONFORMANCE-CASE
// spec: 16-aggregates
// facility: rule.agg.match [Match-Non-Exhaustive] (nested); D-0056; conf.match-nested-non-exhaustive
// kind: negative
// expect: diag.non-exhaustive-match (static)
//
// No arm matches `Ok(None)`: the arms for `Ok` cover only `Ok(Some(_))`.
// The diagnostic names the pattern not covered.

import std;

fn main()
{
    Result<Option<i32>, i32> r = Ok(None);
    i32 v = match (r)
    {
        Ok(Some(x)) : x,
        Err(_) : 0,
    };
}
