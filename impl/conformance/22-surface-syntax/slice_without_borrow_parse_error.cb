// CONFORMANCE-CASE
// spec: 22-surface-syntax
// facility: spec/16 [Slice-Not-Borrowed]; D-0047
// kind: negative
// expect: parse-error
//
// A slice is a borrow: `a[0..2]` without `&` does not parse.

fn main()
{
    auto a = [1, 2, 3];
    auto s = a[0..2];
}
