// CONFORMANCE-CASE
// spec: 22-surface-syntax
// facility: spec/22 §1 byte-char (lexical ill-formedness)
// kind: negative
// expect: parse-error
//
// A byte-literal admits only ASCII source characters; a multi-byte
// character has no single byte value to denote and is ill-formed there
// (spell its bytes with `\xNN`, or use a str-literal).

fn main()
{
    auto b = b"é";
}
