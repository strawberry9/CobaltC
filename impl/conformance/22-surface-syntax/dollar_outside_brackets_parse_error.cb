// CONFORMANCE-CASE
// spec: 22-surface-syntax
// facility: spec/16 rule.agg.dollar; D-0047
// kind: negative
// expect: parse-error
//
// `$` is the length of what is being indexed, so it is written only
// inside `[…]`.

fn main()
{
    usize n = $;
}
