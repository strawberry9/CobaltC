// CONFORMANCE-CASE
// spec: 06-arithmetic
// facility: rule.arith.literal [Literal-Out-Of-Range]
// kind: negative
// expect: diag.literal-out-of-range (static)
//
// A literal whose value cannot fit its determined type is rejected
// before the program runs at all -- this is a fact about the literal
// and its type, not about runtime values.

fn main()
{
    256: u8;
}
