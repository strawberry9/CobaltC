// CONFORMANCE-CASE
// spec: 06-arithmetic
// facility: rule.type.expected (D-0037); rule.arith.checked [Arith-Checked]
// kind: negative
// expect: diag.arith-overflow (dynamic)
//
// A literal expression computed in its declaration's type, `i32`: the
// product overflows, and the fault is reported at this line in both
// implementations (found by the random differential test, seed 92).

fn main()
{
    i32 x = (2147483359 - 10) * 11;
}
