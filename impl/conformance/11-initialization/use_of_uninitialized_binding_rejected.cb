// CONFORMANCE-CASE
// spec: 11-initialization
// facility: rule.init.definite-assignment [Let-Uninit]
// kind: negative
// expect: diag.use-of-uninitialized (static)
//
// [Let-Uninit] (`τ x;`, no initializer) was entirely unimplemented:
// reading such a binding before any write hit diag.type-mismatch by
// accident (the placeholder value failing arithmetic), not the
// registered diagnostic this rule actually names.

fn main()
{
    i32 x;
    i32 y = x + 1;
}
