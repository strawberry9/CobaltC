// CONFORMANCE-CASE
// spec: 12-type-system
// facility: rule.type.eq [Eq-Fn] (no equality on function values)
// kind: negative
// expect: diag.type-mismatch (static)

fn a() : i32
{
    1
}

fn main()
{
    fn() : i32 f = a;
    bool same = f == f;
}
