// CONFORMANCE-CASE
// spec: 12-type-system
// facility: rule.fn.generic-call [Generic-Call-Uninferable]; conf.generic-call-uninferable-rejected
// kind: negative
// expect: diag.cannot-infer-type-parameter (static)
//
// No explicit type argument, no argument shape and no expected type
// fixes Vec::new's T; a later use never counts (inference is local
// and downward, D-0012/D-0013).

import std;

fn main()
{
    auto bad = Vec::new();
    Vec::push(&mut bad, 1);
}
