// CONFORMANCE-CASE
// spec: 12-type-system
// facility: rule.type.typing [T-While]; rule.module.resolve [Resolve-Unbound]
// kind: negative
// expect: diag.unbound-name (static)
//
// A `while` condition is checked like any expression: an unbound name
// in it is rejected statically, not left for run time (coby) or an
// internal error (cobc).

fn main()
{
    while (nope)
    {
    }
}
